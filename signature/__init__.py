"""
Core Signature module.

Provides encrypted signature storage, smooth canvas capture, and PDF signing
(overlay merge) with optional user name/date labels and externally enforced
label positions.
"""
